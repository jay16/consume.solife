.row{style:"margin-top:10px;"}
  .col-sm-6.col-sm-offset-3
    %h1{style: "text-align:center;margin-top:70px;margin-bottom:30px;"}
      爱生活，爱记录
    %h3 橫向解析你的消费习惯，纵向解释你的资金去向。

- if mobile?
  :css
    .login-inline { display: inline-block; width: 75%;}
    .login-submit { margin-top: -42px; }
- else
  :css
    .login-inline { display: inline; }
    .login-inline .controls .form-input { width: 80%; }
    .form-inputs .controls .form-input { height: 53px; }
.row{style:"margin-top:20px;"}
  .col-sm-4.col-sm-offset-4
    .row
      .col-sm-10.col-sm-offset-1
        = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .form-inputs
            = f.input :email, required: false, autofocus: true, label: false, input_html: { class: "col-sm-12 form-input form-control" }, placeholder: "用户邮箱"
            = f.input :password, required: true, label: false, input_html: { class: "col-sm-8 form-input form-control", style: "border-radius: 5px 0px 0px 5px;" }, wrapper_html: { class: "login-inline" }, placeholder: "登陆密码"
            - f.input :remember_me, as: :boolean if devise_mapping.rememberable?
            = link_to "", id: "loadTrigger", class: "login-submit", onclick: "$('#loadSubmit').trigger('click');", class: "btn btn-primary", style: "height:53px;width:20%; border-radius: 0px 5px 5px 0px;" do
              %i.glyphicon.glyphicon-arrow-right{style: "margin-top: 12px;"}
          - if devise_mapping.rememberable?
            .from-group.checkbox
              %label{:for => "user_remember_me"}
                = f.check_box :remember_me
                = t("simple_form.remember_me")
            
            = f.button :submit, "登 陆", label: false, class: "hidden", id: "loadSubmit"
            
        = render "devise/shared/links"

    :javascript
      $(function(){
          // monitor input change
          function inputMonitor() {
              var isDisabled = false;
              $("input").each(function(){
                  if(!$.trim($(this).val()).length) isDisabled = true;
              })
              console.log(isDisabled);
              if(isDisabled == true)
                  $("#loadTrigger").attr("disabled", "disabled");
              else
                  $("#loadTrigger").removeAttr("disabled");
          }

          inputMonitor();
          $("input").bind("onchange keyup input", function(){
              inputMonitor();
          });
          // end monitor input change
      });

