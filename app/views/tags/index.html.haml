.row
  %p 标签列表 

.row
  .list-header
    .col-sm-6 标签 
    .col-sm-2.center.no-padding 类型
    .col-sm-4.center 操作

- @tags.each do |tag|
  = render partial: "tags/tag", locals: { tag: tag } 

- link_to 'New Tag', new_tag_path
